# formatting fixtures

JSX whitespace golden tests. Each case is a directory with `input.rsx`, `input.jsx` (the React/JSX equivalent) and `expected.txt` (the text nodes the parser must produce). Outou follows React's whitespace rules; a case where the two disagree is a bug in Outou.
