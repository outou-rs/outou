# outou-backend-dioxus

Temporary Dioxus backend. Emits `rsx!` invocations as text through `::outou::__private`.
Does not depend on Dioxus itself; user crates never depend on Dioxus directly.

Phase 0: Week 4 (Gate 2).
