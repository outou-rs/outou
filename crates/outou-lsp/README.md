# outou-lsp

Language server for `outou-rsx`. Proxies rust-analyzer and maps positions through source maps in both directions.
The Week 1 spike (`spikes/rust-analyzer/`) validates the approach before this crate is implemented.

Phase 0: Week 5 (Gate 3).
