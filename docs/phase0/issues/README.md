# Phase 0 issues

One file per work item, in execution order. The same items exist as GitHub issues under the "Phase 0" milestone; these files are the in-repository copy. Each file states its gate and whether it may be dropped if time runs out (see `docs/phase0.md`).
