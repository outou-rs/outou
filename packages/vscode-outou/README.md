# vscode-outou

Visual Studio Code support for `.rsx` files (language id `outou-rsx`). Only the language registration exists for now; the extension gains a client for `outou-lsp` once the server works end to end.
